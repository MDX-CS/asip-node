export default class Parser {
  /**
   * Static constructor.
   *
   * @param   {string} raw  Raw data to be parsed
   * @returns {Object} processed  The processed data
   */
  static process(raw) {
    return `readable data ${raw}`;
  }
}
